.sider
  .inner-menu
    ul 
      li
        a(href=`/${prefixAdmin}/dashboard`) Tổng quan
      if(role.permissions.includes("products_view"))
        li
         a(href=`/${prefixAdmin}/products`) Sản phẩm
      if(role.permissions.includes("products-category_view"))
        li
         a(href=`/${prefixAdmin}/category`) Danh Mục
      if(role.permissions.includes("roles_view"))
        li
          a(href=`/${prefixAdmin}/role`) Nhóm Quyền
      if(role.permissions.includes("roles_permissions")) 
        li 
          a(href=`/${prefixAdmin}/premisson`) Phần Quyền
      if(role.permissions.includes("account_view"))
        li 
          a(href=`/${prefixAdmin}/user`) Tài Khoản